import { Container, Typography } from '@material-ui/core'
import React from 'react'

function CoursesScreen() {
    return (
        <Container maxWidth='md'>
            <Typography variant='h4'>Todos los cursos</Typography>
        </Container>
    )
}

export default CoursesScreen
